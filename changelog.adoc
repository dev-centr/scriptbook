= Changelog

== Unreleased — 2026-09-08

* The ten canonical PlayTime diagrams now ship exact standalone-adaptive, host-themed, and fixed deliveries from `dev-centr/general-knowledge` commit `137492ea980910d1286663654948bd82d2d5ac4b`.
* Stable image paths under `spec/images/` and `docs/modules/ROOT/images/` stay synchronized without duplicating the canonical Mermaid sources or manifests.
* A pinned offline check verifies artifact freshness, XML, accessibility, SVG safety, delivery modes, UTF-8 encoding, and Antora enhancement roles.

== Unreleased — 2026-08-18

* Example playbooks for first-class coding monos (`examples/fonts/install-*.cmk`): Cascadia Mono, JetBrains Mono, Fira Code, Iosevka, Monaspace, JetBrains Mono Nerd Font. Intents (`cli.install`), not host shell translation. Catalog ids in `equivalence-rules-cli`.

== 0.2.0 — 2026-08-13

* Name the three layers: CentrMark (format), PlayTime (engine in `lib/`), Scriptbook (CLI app)
* Intent steps: `::: step [intent="cli.install" tool="gh"]` bound via libequivalence
* Host Awareness: baked probe cascade (`lib/embed/probes.generated.json`); no EE call to learn OS
* `::: choose` / `::: ask` / `when-answer` / `when-context`
* CLI: `--format`, `--catalog`, `--answers`, `--prefer-mutable`
* Sidecar records host snapshot, answers, and resolved command
* Example: `examples/install-gh.cmk` (same file on every OS)
* Specs: `spec/playtime.adoc`, `spec/host-awareness.adoc`, updated playbook profile + sidecar
* Illustrated Antora component (`docs/`) for the org hub; figures for layers, bind flow, facets, Host Awareness ratchet

== 0.1.2 — 2026-08-03

* Relicensed to *CSL-1.0-B* (Centr Source License — Branding); was AGPL-3.0-or-later
* History not rewritten; tags/commits prior to this release remain under AGPL as published

== 0.1.1 — 2026-08-03

* `scriptbook history` — readable last-run table from sidecar index
* `scriptbook prohelp` — discoverability for `.cmk.runs/` plus OS audit surfaces (journal / Event Viewer / syslog)
* `status` clarifies when no run has been recorded yet

== 0.1.0 — 2026-07-31

* Initial release: library + CLI (`run`, `status`, `clean-runs`)
* CentrMark playbook/step profile subset parser
* Sidecar runs under `<file.cmk>.runs/`
* Shell backends: auto / nu / sh / bash / pwsh
* Examples: smoke, gh-pages-https
* Nushell helper module
